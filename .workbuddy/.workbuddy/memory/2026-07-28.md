# 2026-07-28 工作记录

## 任务：按完整模块清单扩充数据可视化大屏平台

既有项目为 React 18 + Vite + TS + Zustand 的低代码大屏设计器（`lowcode-dashboard`），已含拖拽编辑器、8 类 SVG 组件、路由模型、联动引擎、mock 后端、大屏管理/数据源/数据集/组件库/静态资源/运行配置/用户管理等功能页。

本次在**不重写核心**的前提下，按用户给出的全量模块清单做了扩展：

### 1. Mock 后端扩展（src/mock）
- types.ts：新增数据源全类型（DsKind/SQL 多库/SqlVendor/解析模式）、消息通道、地图、全局变量、代码、分类、AI 模型/机器人、孪生模型/场景、IoT 设备/告警、填报/工作流/轮播/插件。
- seed.ts：补充上述域的种子数据（含 91 种预置 3D 模型）。
- client.ts：新增通用 CRUD 工厂（支持扩展域全量增删改查）。
- api.ts：新增各域门面方法。

### 2. 新增 / 增强功能页（src/features，已注册到 registry.ts）
- 数据源配置（重写，覆盖静态/API/SQL[MySQL·SQLServer·PostgreSQL·StarRocks·Oracle]/WebSocket/MQTT/Flow/爬虫解析 + 连通测试）
- 消息推送（企业微信/钉钉/邮件/阿里云短信/腾讯云短信）
- 数据填报、数据工作流
- 组件库（增强：EChart/HTML/Vue/3D 海量内置组件实时预览）、组件菜单
- AI 模型管理、AI 助手（智能问答 + 生成 Vue/EChart/HTML 组件 + 自定义机器人）
- 全局变量、代码仓库
- 地图资源（EChart/高德/百度/腾讯/三方 + 预览）
- 分类标签、大屏分析（ECharts 趋势）
- 插件（我的/市场，可安装卸载）
- 帮助中心、AI 模型平台（跳转）
- 数字孪生 3D（Three.js 实时场景：日照/夜景/雾效 + 模型库）
- 物联组态（设备实时指标跳动 + 多级告警）
- 数据报表（Excel 式零代码网格 + 数据集绑定 + AI 生成）
- 独立部署（导出项目 JSON / 独立 HTML / 数据源配置 / 命令行批量构建）

### 3. 依赖
- 新增 echarts、three（已 npm install）。

### 验证
- `npm run typecheck` 通过；`npm run build` 通过（674 模块，产物 ~1.78MB）。
- 注意：bundle 较大（echarts+three），后续可做 manualChunks 拆包。

### 待办 / 后续
- 编辑器内接入 EChart 组件（目前 EChart 预览在组件库页，编辑器仍是 SVG 组件）。
- 数据源 SQL/WebSocket/MQTT 的实时取数需后端代理（mock 仅模拟）。
- 3D 场景拖拽搭建、关键帧轨迹编辑（当前为配置驱动预览）。
- 报表专业级打印/导出（PDF/Excel）接入。

## git 配置
- 新建 .gitignore（忽略 node_modules/、dist/、日志、.env 等），并用 git rm -r --cached 将 2599 个 node_modules 文件移出 git 跟踪（本地保留）。dist/ 仍有 21 个文件被跟踪，待用户决定是否移除。
- 已用 git rm -r --cached dist 移除 dist 的 21 个跟踪文件，.gitignore 已暂存，node_modules 与 dist 均不再被 git 跟踪。
